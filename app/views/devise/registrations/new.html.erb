 <section class = "p-4 height-700 parallax-bg" style="background-image: url(https://comhelp.s3.eu-west-3.amazonaws.com/comhelp/Front/pier-569314_640.jpg)">
    
    <div class ="card border-primary mx-auto w-25 h-100 ">
      <div class= "d-flex flex-column mt-4">
          <div class="col-sm-4 mx-auto">
            <img src="https://comhelp.s3.eu-west-3.amazonaws.com/comhelp/Logo/imageonline-co-transparentimage.jpg" alt="logo" class="img-responsive">
          </div>

          <h2 class ="text-center my-3"> S'inscrire </h2>
    
          <div class="form mx-auto w-75">
            <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
              <%= render "devise/shared/error_messages", resource: resource %>
              <div class="field">
                <%= f.label :Prénom %></br>
                <%= f.text_field :first_name, autofocus: true, autocomplete: "Prénom", :class => "mb-1" %>
              </div>

              <div class="field">
                <%= f.label :Nom %></br>
                <%= f.text_field :last_name, autofocus: true, autocomplete: "Nom", :class => "mb-1" %>
              </div>

              <div class="field">
                <%= f.label :email %></br>
                <%= f.email_field :email, autofocus: true, autocomplete: "email", :class => "mb-1" %>
              </div>

              <div class="field">
                <%= f.label :password %>
                <% if @minimum_password_length %>
                <em><%= t('devise.shared.minimum_password_length', count: @minimum_password_length) %></em>
                <% end %><br />
                <%= f.password_field :password, autocomplete: "new-password" %>
              </div>

              <div class="field">
                <%= f.label :password_confirmation %><br />
                <%= f.password_field :password_confirmation, autocomplete: "new-password" %>
              </div>

              <div class="actions">
                <%= f.submit "S'inscrire", :class => "mx-auto"%>
              </div>
            <% end %>
            <div>
              <%= render "devise/shared/links" %>
            </div>
          </div>
      </div>
    </div>

